export declare class Shibboleth {
    private _shibURL;
    constructor(shibURL: string);
    hasShibSessionInfo(req: any, headers?: string[]): boolean;
    shouldRedirect(req: any): boolean;
    redirect(req: any, res: any, next: any): void;
}
export declare function shibboleth(req: any, res: any, next: any): void;
